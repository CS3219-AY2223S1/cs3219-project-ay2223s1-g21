const SERVICE_HEALTHY = "Matching service is healthy";
const INTERVIEW_ENDED = "Interview session has ended";
const CANCELLED_FIND_MATCH = "Cancelled find match successfully";

module.exports = {
    SERVICE_HEALTHY,
    INTERVIEW_ENDED,
    CANCELLED_FIND_MATCH
};